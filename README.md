# Marketplace Product Form

Форма создания товара (nomenclature) для маркетплейса на TableCRM API.

## Стек

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **react-hook-form** + **zod** — валидация
- **sonner** — toast-уведомления
- **Radix UI** — примитивы (tabs, select, label)

## Запуск локально

```bash
# 1. Установить зависимости
npm install

# 2. Создать .env.local
cp .env.example .env.local
# Вставить токен: NEXT_PUBLIC_TABLECRM_TOKEN=xxxxx

# 3. Запустить dev-сервер
npm run dev
```

## Деплой на Vercel

1. Залить репозиторий на GitHub
2. Импортировать проект в [Vercel](https://vercel.com)
3. В настройках проекта добавить Environment Variable:
   - `NEXT_PUBLIC_TABLECRM_TOKEN` = ваш токен TableCRM
4. Deploy 🚀

## Функциональность

### Форма разбита на 4 вкладки:
- **Основное** — название, артикул, цена, единица измерения, категории, кешбэк
- **Описание** — краткое и полное описание
- **SEO** — title, description, keywords (теги)
- **Локация** — адрес, координаты

### UX-фичи:
- ✅ Автосохранение черновика в localStorage
- ✅ Кнопка восстановления черновика
- ✅ Генерация SEO из названия товара (умный мок)
- ✅ Индикатор ошибок на вкладках (красная точка)
- ✅ Loading-состояние при отправке
- ✅ Toast-уведомления (успех / ошибка)
- ✅ Навигация вперёд/назад между вкладками
- ✅ Кнопки: Создать / Черновик / Сброс

### Валидация (zod):
- `name`: обязательное, 2–120 символов
- `code`: до 64 символов, без пробелов в начале/конце
- `marketplace_price`: ≥ 0
- `chatting_percent`: 0–100
- `seo_keywords`: 0–20 тегов, каждый 1–32 символа
- `latitude`: -90..90
- `longitude`: -180..180
- `unit`, `category`, `global_category_id`: положительные числа
